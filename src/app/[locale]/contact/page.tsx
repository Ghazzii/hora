import { InfoPage } from "@/components/site/InfoPage";
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return <InfoPage eyebrow="Contact" title={locale === "fr" ? "Parlons de votre montre" : "Let's talk about your watch"}><p>{locale === "fr" ? "Ajoutez ici le numéro de téléphone, l'e-mail et les horaires officiels de Hora avant lancement." : "Add Hora's official phone number, email and opening hours here before launch."}</p><p className="border-l-2 border-gold pl-4 text-sm"><strong>{locale === "fr" ? "À vérifier avant publication :" : "Review before publishing:"}</strong> {locale === "fr" ? "coordonnées commerciales et délais de réponse." : "business contact details and response times."}</p></InfoPage>; }
