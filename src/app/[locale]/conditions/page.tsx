import { InfoPage } from "@/components/site/InfoPage";
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return <InfoPage eyebrow={locale === "fr" ? "Juridique" : "Legal"} title={locale === "fr" ? "Conditions de vente" : "Terms of sale"}><p>{locale === "fr" ? "Les commandes sont enregistrées dans Hora, confirmées par téléphone, préparées puis expédiées. Le paiement est dû au livreur." : "Orders are recorded in Hora, confirmed by phone, prepared and shipped. Payment is due to the courier."}</p><p className="border-l-2 border-gold pl-4"><strong>{locale === "fr" ? "Modèle à faire valider :" : "Template requiring review:"}</strong> {locale === "fr" ? "ajoutez l'identité légale, les garanties, la rétractation, les retours et le règlement des litiges." : "add legal identity, warranties, withdrawal, returns and dispute resolution."}</p></InfoPage>; }
