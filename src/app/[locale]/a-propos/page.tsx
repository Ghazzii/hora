import { InfoPage } from "@/components/site/InfoPage";
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return <InfoPage eyebrow="Hora" title={locale === "fr" ? "Une maison tunisienne contemporaine" : "A contemporary Tunisian watch house"}><p>{locale === "fr" ? "Hora imagine des montres accessibles, conçues autour de la lumière, des couleurs et de l'architecture tunisiennes." : "Hora creates accessible watches inspired by Tunisia's light, colors and architecture."}</p><p>{locale === "fr" ? "Cette boutique est indépendante : ses produits, son stock et ses commandes vivent dans la base de données Hora." : "This store is independent: its products, inventory and orders live in Hora's own database."}</p></InfoPage>; }
