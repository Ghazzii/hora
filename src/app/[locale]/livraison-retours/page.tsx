import { InfoPage } from "@/components/site/InfoPage";
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return <InfoPage eyebrow={locale === "fr" ? "Service" : "Service"} title={locale === "fr" ? "Livraison & retours" : "Delivery & returns"}><p>{locale === "fr" ? "Livraison dans les 24 gouvernorats au tarif fixe de 8 DT. Le paiement s'effectue à la livraison après confirmation téléphonique." : "Delivery to all 24 governorates for a fixed 8 DT fee. Payment is made on delivery after phone confirmation."}</p><p>{locale === "fr" ? "Les délais, conditions de refus et procédure de retour doivent être complétés selon votre transporteur et votre politique commerciale avant lancement." : "Timelines, refusal conditions and the return procedure must be completed for your carrier and commercial policy before launch."}</p></InfoPage>; }
