import { InfoPage } from "@/components/site/InfoPage";
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return <InfoPage eyebrow={locale === "fr" ? "Juridique" : "Legal"} title={locale === "fr" ? "Confidentialité" : "Privacy"}><p>{locale === "fr" ? "Hora enregistre les informations nécessaires à la commande, à la livraison, au service client et à la sécurité du compte. Les paramètres de campagne sont facultatifs." : "Hora stores the information needed for ordering, delivery, customer service and account security. Campaign parameters are optional."}</p><p className="border-l-2 border-gold pl-4"><strong>{locale === "fr" ? "Modèle à faire valider :" : "Template requiring review:"}</strong> {locale === "fr" ? "complétez le responsable de traitement, les durées de conservation, les droits et le contact avant lancement." : "complete the data controller, retention periods, rights and contact details before launch."}</p></InfoPage>; }
