# Verification record

The repository includes automated verification in `.github/workflows/ci.yml`:

- PostgreSQL 16 service health
- Prisma generation, migration and idempotent seed
- ESLint
- strict TypeScript
- Vitest unit tests
- Next.js production build
- Chromium Playwright smoke tests

This file is updated with exact workflow results after the first source commit. It intentionally makes no passing claim before CI completes.
